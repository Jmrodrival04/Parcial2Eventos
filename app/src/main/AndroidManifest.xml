<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.parcial2eventos20">

    <!-- Permisos requeridos -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Definición de la aplicación -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Parcial2Eventos20">

        <!-- Actividad principal (Pantalla principal) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad para horarios -->
        <activity
            android:name=".HorarioActivity"
            android:exported="false"
            android:label="@string/horario_activity_label" />

        <!-- Actividad para eventos -->
        <activity
            android:name=".EventosActivity"
            android:exported="false"
            android:label="@string/eventos_activity_label" />

        <!-- Actividad para farmacias -->
        <activity
            android:name=".FarmaciasActivity"
            android:exported="false"
            android:label="@string/farmacias_activity_label" />

    </application>
</manifest>
